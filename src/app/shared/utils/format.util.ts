// src/app/shared/utils/format.util.ts

/**
 * A utility file for various formatting functions.
 * This can be expanded with functions for dates, numbers, strings, etc.
 *
 * Example function:
 * export function formatDate(date: Date, format: string): string {
 *   // Implementation here
 * }
 */
