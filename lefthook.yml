# Configuración de Lefthook para Git Hooks
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    biome:
      glob: "**/*.{ts,js,json}"
      run: npx @biomejs/biome check --write --staged --no-errors-on-unmatched
      stage_fixed: true
